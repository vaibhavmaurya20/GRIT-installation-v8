// GCD and LCM
fn gcd a:int b:int -> int
    if b == 0 then a else gcd(b, a % b)

fn lcm a:int b:int -> int
    a / gcd(a, b) * b

fn main
    println(gcd(48, 18))
    println(gcd(100, 75))
    println(lcm(4, 6))
    println(lcm(12, 8))
